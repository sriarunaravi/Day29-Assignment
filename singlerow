use Shipping;
SELECT Product_Name, Price
FROM Shipping_Table
WHERE Price = (
    SELECT min(Price)
    FROM Shipping_Table
);
select Product_Name,Price from Shipping_Table where Category in (select Category from Shipping_Table where Category='Footwear');
